var express = require('express');
var router = express.Router();

/* GET home page. */
// router.get('*', function(req, res, next) {
//   // Redirect to API until Angular App is set up...
//   res.redirect('/api/v1/memories');
// });

module.exports = router;
